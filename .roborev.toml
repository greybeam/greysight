review_agent = "claude-code"
review_guidelines = """
In code review, keep in mind we always opt for DRY and simple code. If the lazy way is possible, that often tends to be the best way to do something.
Code should be written for maintainability and ease of use for future agent and human engineers.
For Python files (.py), delegate to the python-reviewer subagent.
For SQL or database-related files, delegate to the database-reviewer subagent.
For specs, plans, and anything architecture and design related, delegate to the architect agent.
Always run the security-reviewer subagent on any code touching authentication,
credentials, or external data boundaries.
"""
